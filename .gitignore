# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Environments
.venv/
.env
.envrc

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Simulation outputs
runs/

# Generated submission media artifacts
media/

# Scenario pack raw caches (gitignored; license requirement for some adapters)
scenarios/*/raw/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Browser-automation session artifacts (never commit)
.playwright-mcp/

# Orchestration/session state (never commit)
.omc/
.claude/

# Local agent guidance (not published)
CLAUDE.md

# Internal handoff doc — contains deploy topology & infra details (host IPs, SSH user,
# deploy paths), so it stays local. FIELD-NOTES.md is published: the README and source
# cite it by section, and it carries no infra detail (host nicknames genericized).
docs/FOLLOWUPS.md

# Frontend build artifacts
web/node_modules/
web/dist/
# Playwright E2E artifacts (never commit)
web/test-results/
web/playwright-report/

# Agent-skills installers (keep third-party skills out of the repo)
.agents/
skills-lock.json
