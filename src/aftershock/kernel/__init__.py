"""The reusable agent-society kernel.

Import from submodules directly (e.g. ``from aftershock.kernel.protocol import
Observation``); this package init deliberately stays empty so modules can be
developed and tested independently.
"""
