"""LLM-driven agents: the Qwen provider chokepoint, observation digests,
output parsing, and the LLMAgent that turns a role spec into a society member.

Import from submodules directly; this init deliberately stays empty.
"""
